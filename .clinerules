# 绝对禁止的命名规则

1. 本项目是专门为“粘人精”角色打造的私有项目。
2. 绝对、严禁、不可在任何地方（代码、变量名、文件名、注释等）使用“Kiro”、“Krio”、“周棋洛”等任何相关的英文名或拼音！
3. 若需要命名前缀，请使用 `clingy_` 或 `app_` 等通用命名。
4. 任何时候写新的代码，都必须在文件顶部加上此警告注释。

# 绝对禁止的存储规则

1. 严禁使用 localStorage 存储 Base64 格式的图片数据！这极易导致 QuotaExceededError。
2. 凡是涉及图片存储（头像、世界书封面等），必须统一使用 localforage 存入 IndexedDB（参考小组件 `DualAvatarWidget.vue` 方案），或保存为其它的原生安全格式。

# 绝对禁止的架构与文件堆积规则

1. 单文件体积限制：任何 Vue 组件或脚本文件（包括代码与样式）的大小应尽可能保持在 50KB 以下（控制在 300-500 行以内）。
2. 弹窗（Modal）抽离原则：当一个主组件中包含超过一个逻辑复杂的弹窗时，必须将弹窗拆分为独立的子组件（存放在 `components/xxx/modals/` 目录下）。
3. 状态与逻辑抽离原则（Composables）：当组件内的 `<script setup>` 逻辑超过 150 行时，必须将相关的数据状态、方法提取到独立的组合式函数（`src/composables/useXxx.ts`）中。主文件仅负责引入和组装。
4. 必须始终保持编码格式为 UTF-8。

# 绝对禁止的 UI 与交互规则

1. 严禁使用浏览器默认的弹窗（如 `alert`、`confirm`、`prompt` 等），必须统一使用自定义的美化弹窗组件。
